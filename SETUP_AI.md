# Настройка AI Рекомендаций

## 1. Установка зависимостей

```bash
pip install openai
```

## 2. Получение API ключа OpenAI

1. Зарегистрируйтесь на https://platform.openai.com/
2. Создайте API ключ в разделе API keys
3. Скопируйте ключ (начинается с `sk-...`)

## 3. Установка API ключа

⚠️ **ВАЖНО:** Используйте `.env` файл - он безопасный и удобный!

### Вариант 1: Через `.env` файл (РЕКОМЕНДУЕТСЯ)

1. Откройте файл `.env` в корне проекта
2. Замените плейсхолдер на ваш реальный ключ:
   ```
   OPENAI_API_KEY=sk-ваш-настоящий-ключ-сюда
   ```
3. Сохраните файл
4. Готово! `.env` в `.gitignore` и не попадет в Git

**Подробная инструкция:** см. файл `INSTALL_API_KEY.md`

### Вариант 2: Системные переменные Windows

1. Win + Pause/Break → Дополнительные параметры системы
2. Переменные среды → Создать
3. Имя: `OPENAI_API_KEY`, Значение: `sk-ваш-ключ`
4. ОК, перезапустить терминал

### Вариант 3: Временно в PowerShell

```powershell
$env:OPENAI_API_KEY="sk-ваш-ключ-здесь"
python server.py
```

## 4. Проверка установки

```bash
echo %OPENAI_API_KEY%    # Windows
echo $OPENAI_API_KEY     # Linux/Mac
```

## 5. Запуск приложения

```bash
python server.py
```

Откройте `index.html` в браузере.

## 6. Использование AI рекомендаций

1. Заполните форму:
   - Модель: `Yamaha XT690Z TENERE 700`
   - Год: `2025`
   - Пробег: `25`
   - Оценка: `7`
   - Стартовая цена: `1296163`

2. Нажмите "Получить рекомендацию AI"

3. Подождите ~10-15 секунд

4. Получите:
   - Минимальную ставку
   - Оптимальную ставку
   - Максимальную ставку
   - Детальный анализ с коэффициентами
   - Предупреждения (если есть)

## Стоимость

- Модель: `gpt-4o`
- Стоимость: ~$0.01-0.02 за запрос
- Данные: передаётся до 1000 исторических лотов

## Алгоритм

AI анализирует:
1. Фильтрует аналогичные модели из БД
2. Учитывает год ±1-2, близкий пробег, оценку
3. Рассчитывает коэффициенты (Цена/Стартовая) для аналогов
4. Удаляет выбросы (IQR метод)
5. Выбирает перцентиль (p60-p85) в зависимости от состояния
6. Умножает стартовую цену на коэффициенты
7. Возвращает диапазон ставок

## Troubleshooting

**Ошибка: "OPENAI_API_KEY не установлен"**
→ Установите ключ (см. пункт 3), перезапустите сервер

**Ошибка: "OpenAI библиотека не установлена"**
→ Выполните `pip install openai`

**Ошибка: "Rate limit exceeded"**
→ Подождите минуту, у вас закончились бесплатные токены или превышен лимит

**Ошибка: "Invalid API key"**
→ Проверьте правильность ключа

