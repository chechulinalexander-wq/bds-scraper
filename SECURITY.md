# Безопасность API ключа

## ✅ Правильный способ (используется сейчас)

API ключ хранится в файле `.env` в корне проекта:

```
OPENAI_API_KEY=ваш-ключ
```

### Преимущества:
- ✅ Ключ не виден в коде
- ✅ Файл `.env` добавлен в `.gitignore` (не попадет в git)
- ✅ Можно безопасно делиться проектом
- ✅ Легко менять ключ (редактируете `.env`)

## 📁 Структура файлов

```
BDS - Scraper/
├── .env                 ← Ваш секретный файл с ключом
├── .env.example         ← Пример файла (без ключа)
├── .gitignore          ← Защита от попадания в git
├── server.py           ← Читает из .env
└── start_server.bat    ← Просто запускает сервер
```

## 🔄 Как обновить ключ

1. Откройте файл `.env` в блокноте
2. Замените ключ на новый
3. Сохраните
4. Перезапустите `start_server.bat`

## 🚫 Что НЕ ДЕЛАТЬ

❌ Не добавляйте `.env` в git  
❌ Не публикуйте ключ в коде  
❌ Не отправляйте `.env` кому-либо  
❌ Не храните ключ в скриншотах  

## 🔒 Если ключ утек

1. Удалите старый ключ на https://platform.openai.com/api-keys
2. Создайте новый
3. Обновите `.env`
4. Перезапустите сервер

## 📝 Для других разработчиков

Если вы клонируете проект:

1. Скопируйте `.env.example` в `.env`
   ```bash
   copy .env.example .env
   ```
2. Вставьте свой API ключ в `.env`
3. Запустите `start_server.bat`

---

**Теперь ваш ключ в безопасности!** 🔐

